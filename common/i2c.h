// Includes (if needed)
#include <stdint.h>  // Example: Standard integer types

// Macros (if needed)
#define COMMON_MACRO 42  // Example macro definition

// Type definitions (if needed)
typedef struct {
    int field1;
    float field2;
} CommonStruct;

// Function declarations
void common_function(void);
int add_numbers(int a, int b);